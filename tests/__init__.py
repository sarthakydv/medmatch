"""Test suite for the Medical Entries Data Pipeline & Search API.

Feature-specific tests (loader, index, API) land with their features
(feat-003, feat-004, feat-005). This package currently holds only a smoke test
so ``pytest`` has something to run during ``feat-001`` bootstrap.
"""
